<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://apache.org" 
         xmlns:xsi="http://w3.org"
         xsi:schemaLocation="http://apache.org https://apache.org">
    <modelVersion>4.0.0</modelVersion>
    
    <!-- Aquí defines la versión global de Spring Boot para todos tus microservicios -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.4</version> 
        <relativePath/>
    </parent>

    <groupId>com.example</groupId>
    <artifactId>ecommerce-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging> <!-- IMPORTANTÍSIMO: Indica que es un contenedor, no una app ejecutable -->

    <!-- Aquí dentro meterás las carpetas de tus microservicios -->
    <modules>
        <!-- <module>customer-service</module>
        <module>order-service</module> -->
        <module>auth-service</module>
    </modules>

    <properties>
        <java.version>21</java.version>
    </properties>
</project>
